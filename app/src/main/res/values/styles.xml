<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="linearlayout">
        <item name="android:orientation">vertical</item>
        <item name="android:gravity">center_horizontal|start</item>
        <item name="android:background">@android:color/white</item>
    </style>


    <style name="imagebutton" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <style name="googleImage">
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="text">
        <item name="android:gravity">center</item>
        <item name="android:textStyle">italic</item>

        <item name="android:textColor">@color/colorPrimaryDark</item>

    </style>

    <style name="linearlayout_below">
        <item name="android:orientation">horizontal</item>

    </style>

    <style name="rel_lay">
        <item name="android:layout_gravity">center_vertical</item>
    </style>

    <style name="text_description">
        <item name="android:gravity">start|center_vertical</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
